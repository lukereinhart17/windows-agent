fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pyautogui==0.9.54
mss==9.0.2
websockets==13.1
google-generativeai==0.8.5
python-dotenv==1.0.1

# --- Optional: CNN model dependencies (install only what you need) ---
# Faster R-CNN, ResNet, EfficientNet, MobileNet, ShuffleNet:
#   pip install torch torchvision
# ResNet/EfficientNet via timm:
#   pip install timm
# YOLO:
#   pip install ultralytics
# CNNParted:
#   git clone https://github.com/2Moles/CNNParted.git && cd CNNParted && pip install -e .
